# 在 /opt/caddy/Caddyfile 的末尾追加以下内容：
# 注意：不要覆盖已有的其他域名配置！

image.lshang.top {
    reverse_proxy gptimage-app:3000

    # 图片上传大小限制（图生图需要）
    request_body {
        max_size 20MB
    }

    # 头信息透传
    header {
        X-Forwarded-Proto https
        X-Forwarded-Host {host}
    }
}
